import { SuccessEventsGenerator } from '../../helpers/globalInterfaces';

export const data: SuccessEventsGenerator = {
    events: [
        {
            count: 6,
            event_type: 'task_completed'
        }
    ]
};
