import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    textField: {
        width: '80%'
    },
    grid: {
        minHeight: '100vh'
    },
    error: {
        marginTop: '20px',
        color: 'red'
    }
}));
